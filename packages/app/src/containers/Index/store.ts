import Action from '../../types/Action';

const initialState = {
};

export const reducer = (state = initialState, action: Action) => {
  switch (action.type) {
    default:
      return state;
  }
};
